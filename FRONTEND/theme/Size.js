const RATE = 1;
const Size = {
  // Text size
  FontTiny: RATE * 10,
  FontSmallest: RATE * 12,
  FontSmaller: RATE * 14,
  FontSmall: RATE * 15,
  FontMedium: RATE * 16,
  FontBig: RATE * 18,
  FontBigger: RATE * 20,
  FontBiggest: RATE * 22,
  FontHuge: RATE * 24,
  FontHuger: RATE * 26,
  FontHugest: RATE * 35,
  FontGreat: RATE * 40,
  FontGreater: RATE * 60,

  // General
  LayoutTiny: RATE * 0.5,
  LayoutSmallest: RATE * 1,
  LayoutSmaller: RATE * 2,
  LayoutSmall: RATE * 4,
  LayoutMedium: RATE * 8,
  LayoutBig: RATE * 12,
  LayoutBigger: RATE * 18,
  LayoutBiggest: RATE * 20,
  LayoutHuge: RATE * 26,
  LayoutHuger: RATE * 36,
  LayoutHugest: RATE * 48,
  LayoutGreat: RATE * 54,
  LayoutGreater: RATE * 60,

  BackButtonWidth: RATE * 40,
  DividerLine: RATE * 1,

  ButtonWidth: RATE * 100,
  ButtonHeight: RATE * 40,
  ButtonCorner: RATE * 4,
  ButtonBorderWidth: RATE * 1,
  ButtonActionSize: RATE * 36,

  TextMargin: RATE * 8,
  TextPadding: RATE * 10,
  TextInputHeight: RATE * 50,
  TextInputCorner: RATE * 5,

  IndicatorSize: RATE * 32,
  ExpandableViewHeight: RATE * 30,
  MainFrameHeaderHeight: RATE * 200,
};

// const Phone = {
//   BottomMenuHeight: 56,
// };

// const Tablet = {
//   BottomMenuHeight: 80,
// };

// const Size =  Common;

export default Size;
